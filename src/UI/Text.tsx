import { Typography } from "@mui/material";

const Text = () => {
  return (
    <div id="text" className="panel">
      <Typography variant="h3" color="white">
        Your Company
      </Typography>
    </div>
  );
};

export default Text;
